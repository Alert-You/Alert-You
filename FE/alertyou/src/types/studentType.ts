export interface studentsType {
  studentId: number,
  name: string,
  phone: string,
  isGuard: Boolean
}

export interface studentType {
  school: string,
  name: string,
  role: string,
  phone: string
}