export interface verifyResponseType {
  certNumber: string,
  message: string,
  statusCode: number
}

export interface signUpResponseType {
  message: string,
  signupRes: boolean,
  statusCode: number
}