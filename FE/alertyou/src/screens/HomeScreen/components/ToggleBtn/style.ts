import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    borderWidth: 1,
    borderRadius: 1000,
    marginVertical: 10,
    marginBottom: 30,
  },

  container2: {
    elevation: 2,
  },

  title: {
    fontSize: 20,
    fontWeight: 'bold',
    color: 'white',
    marginBottom: 12,
  },

  paragraph: {
    fontSize: 15,
    color: 'white',
    textAlign: 'center',
  },
});
